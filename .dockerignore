# Build context is this app's own directory (apps/agency_tracking/) -- see the note at the
# top of the Dockerfile. Keep the context small and don't leak local dev/build artifacts in.
.git
.github

__pycache__
*.pyc
node_modules
frontend/node_modules
frontend/dist

*.log
